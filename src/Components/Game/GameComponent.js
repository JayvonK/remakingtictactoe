//Pseudo
//Creating a component that returns a parent div, with a child div, that has and title of tic tac toe, theres another child div that has a boardcomponent as it's grandchild, with the properties of squares and an onclick
//At the top we use destructuring to assign history, stepNumber, updateHistory, resetHistory, and setStepNumber to access all the returns from our useGameHistory() function
//We set currentSquares equal to 



import React from 'react'

const GameComponent = () => {
  return (
    <div>
      
    </div>
  )
}

export default GameComponent
